---
hello_world:
  module: hello
  class: HelloWorld

zmci_mqtt:
  module: zmci_mqtt_ha
  class: ZeroCloudInterfaceHA
  log: zmci
  user_name: !secret ZERO_USER
  user_pass: !secret ZERO_PASS
  mqtt_broker: !secret MQTT_HOST
  mqtt_user: !secret MQTT_USER
  mqtt_pass: !secret MQTT_PASS
